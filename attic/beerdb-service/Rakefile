require 'hoe'
require './lib/beerdb/service/version.rb'

Hoe.spec 'beerdb-service' do

  self.version = BeerDbService::VERSION

  self.summary = 'beerdb-service gem - beer.db addon for web service (HTTP JSON API)'
  self.description = summary

  self.urls = ['https://github.com/beerkit/beer.db.service.ruby']

  self.author = 'Gerald Bauer'
  self.email = 'beerdb@googlegroups.com'

  # switch extension to .markdown for gihub formatting
  self.readme_file = 'README.md'
  self.history_file = 'HISTORY.md'

  self.licenses = ['Public Domain']

  self.spec_extras = {
   :required_ruby_version => '>= 1.9.2'
  }

end
